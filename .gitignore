# Dependencies
node_modules/

# Build output
# dist/ is now tracked — it contains the pre-built site for Cloudflare Workers deployment
# This ensures cat.mp4 and all assets are uploaded, not re-built from scratch
# build/

# Environment files (keep .env.example only)
.env
.env.local
.env.*.local

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Large media files (keep links in code, but don't commit actual files)
*.mp4
*.mov
*.avi
*.webm
*.zip
*.tar
*.gz

# Cloudflare
.wrangler/
wrangler.jsonc

# Vite cache
.vite/

# TypeScript cache
*.tsbuildinfo

# Project-specific (local only)
history.txt
entities.json
metadata.json